# Контекст работы от 04.05.2026

## ✅ Что уже сделано:

### 1. Markdown парсер (`markdownparser.cpp/h`)
- Заголовки (`# H1`...`### H3`)
- Жирный (`**текст**`), курсив (`*текст*`), код (`` `code` ``)
- Списки (маркированные и нумерованные)
- Цитаты (`> текст`)
- Блоки кода (``` ... ```)
- **Таблицы** (| col1 | col2 |) — через `QTextTable`
- **Ссылки** (`[text](url)`) и **картинки** (`![alt](url)`)
- Горизонтальные линии (`---`, `***`, `___`) — исправлено 04.05 вечером

### 2. Двусторонняя конвертация
- `parseToDocument()` — Markdown → QTextDocument
- `documentToMarkdown()` — QTextDocument → Markdown (с таблицами и форматированием)

### 3. Интерфейс (`mainwindow.cpp/h`)
- Переключение между Markdown и WYSIWYG режимами
- Кнопки **Undo/Redo** на панели инструментов
- Меню с горячими клавишами (Ctrl+Z, Ctrl+Y)

## 🔴 Что осталось (Сложное):

### 1. Undo/Redo при переключении режимов (САМАЯ СЛОЖНАЯ ЗАДАЧА)
- Сейчас история работает только внутри одного режима
- При переключении Markdown → WYSIWYG создается НОВЫЙ QTextDocument
- Нужно архитектурное решение для сохранения истории
- Возможное решение: использовать один QTextDocument для обоих режимов

### 2. Позиционирование таблицы
- Замечание: таблица "на верху пирамиды" (позиция курсора)
- Нужно исправить позицию после вставки таблицы

### 3. Вложенное форматирование
- Парсер есть (`processInline` с рекурсией), но может глючить
- Пример: `**bold *italic* bold**`

## 📂 Файлы для работы:

1. `F:\Github\Edit-Markdown\markdownparser.cpp` — основной парсер
2. `F:\Github\Edit-Markdown\markdownparser.h` — заголовочный файл парсера
3. `F:\Github\Edit-Markdown\mainwindow.cpp` — логика Undo/Redo (строки ~1015-1100)
4. `F:\Github\Edit-Markdown\mainwindow.h` — объявления

## 🛠️ Команды:

### Сборка:
```powershell
cd "F:\Github\Edit-Markdown\build_cmake_new"
cmake --build . --config Release
```

### Запуск:
```powershell
cd "F:\Github\Edit-Markdown\build_cmake_new\application"
.\markdown-editor.exe
```

### Если заблокирован exe:
```powershell
taskkill /im markdown-editor.exe /f
```

## 🎯 План на завтра (05.05.2026):

1. **Сначала исправить таблицу** (позиционирование — "верх пирамиды")
2. **Попробовать решить Undo/Redo между режимами** (архитектурно)
3. Протестировать вложенное форматирование

## 📝 Замечания пользователя:
- Блок 9 (Horizontal rules) с `---`, `***`, `___` плохо парсился — **исправлено**
- Некоторые сообщения на китайском (ошибка локализации) — **исправлено**

---
**Спокойной ночи! Завтра продолжим с таблицы 🚀**
